<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <defs>
    <radialGradient id="orbGrad" cx="35%" cy="35%" r="80%">
      <stop offset="0%" stop-color="#e9d5ff"/>
      <stop offset="45%" stop-color="#a855f7"/>
      <stop offset="100%" stop-color="#4c1d95"/>
    </radialGradient>
  </defs>
  <!-- Soft glow: layered translucent circles instead of a blur filter (stable as a cursor) -->
  <circle cx="16" cy="16" r="13.5" fill="#a855f7" opacity="0.12"/>
  <circle cx="16" cy="16" r="10.5" fill="#c084fc" opacity="0.18"/>
  <!-- Orbit ring -->
  <circle cx="16" cy="16" r="11" fill="none" stroke="#a855f7" stroke-width="1.4" opacity="0.55"/>
  <!-- Glowing planet core -->
  <circle cx="16" cy="16" r="7" fill="url(#orbGrad)" stroke="#ffffff" stroke-width="1.5"/>
  <!-- Sparkle -->
  <path d="M26 6 L26.9 8.1 L29 9 L26.9 9.9 L26 12 L25.1 9.9 L23 9 L25.1 8.1 Z" fill="#e9d5ff" opacity="0.9"/>
</svg>
