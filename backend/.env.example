# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb+srv://kali:4M7C9h8lSmHZiArh@smartenglish.lhdsekp.mongodb.net/?appName=smartenglish

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Speaking Coach AI — Google Gemini FREE tier (1,500 requests/day, no card)
# Get a key in 2 minutes at https://aistudio.google.com/apikey
# OPTIONAL: speaking practice works without it (limited fallback feedback),
# but grammar & word-choice feedback is much richer with a key.
GEMINI_API_KEY=

# Speaking Coach Speech-to-Text — Groq FREE Whisper (~2,000 req/day, no card)
# Get a key in 2 minutes at https://console.groq.com/keys
# REQUIRED for the mic-based speaking flow to work (transcribes audio).
GROQ_API_KEY=